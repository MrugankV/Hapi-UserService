module.exports = {
  testEnvironment: "node", // Use the node environment for testing
  //   setupFilesAfterEnv: ["<rootDir>/setupTests.js"], // Optional: For any setup steps
  moduleDirectories: ["node_modules", "src"], // Resolve modules from these directories
};
